---
name: grill-me
description: Start a deliberate planning interview that stress-tests a plan, design, decision, or idea before implementation. Use only when the user explicitly asks to be grilled, says "grill me", or invokes this skill.
---

# Grill me

Use this as the manual entry point for the `grilling` workflow.

When invoked, run the `grilling` skill if the runtime provides a Skill tool. If it does not, apply the `grilling` workflow described in the sibling skill directory.

This is a planning gate. Do not implement, edit files, create issues, commit, or push while the design is still being resolved.
